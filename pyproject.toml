[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]
pythonpath = [
    ".",
]

# Install dev dependencies for local testing:
#   pip install -r requirements-dev.txt
